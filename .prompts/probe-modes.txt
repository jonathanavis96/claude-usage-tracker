You are working for Jonathan Avis (surname Avis; "grafe" and "jonathan" are only usernames) on the private repo claude-usage-tracker. You are in a git worktree on branch `probe-modes` (Python 3 stdlib only, tests run with `python3 -m unittest discover -s tests`). Do NOT dispatch sub-agents; do all the work yourself. Do NOT run a real probe against any account (no `--account`, no live `claude` calls): the account you are running on is the one the probe measures, and your own usage would corrupt the reading. Unit tests with fake `run`/`read` callables only (see tests/test_probe.py for the pattern). Do NOT touch the checkout at ~/claude-usage-tracker; it is the cron working tree.

Background. `tracker/probe.py` `run_tick_probe` fires identical ~42k-token prompts at the Claude CLI one at a time and reads the 5-hour usage meter after each, until the meter has advanced `ticks` percent past the first observed tick. It reports tokens spent per 1%. Measured facts from the 2026-09-06 09:39 Sonnet run: the first span after alignment (tick 1 to tick 2) took 5 prompts while the next four took 9, 10, 9, 10. So the first post-alignment span is suspect (meter catch-up), later spans are steady within one prompt. Also each prompt currently costs about 40 s of model time plus a fixed `settle_s=60` sleep before the meter is read, so a 48-prompt run takes 80 minutes.

Build three additions to `run_tick_probe` and the CLI (`tracker/probe.py` `main`), keeping every existing default and every existing test green:

1. `--skip N` (default 0): after the first observed tick, discard the spend of the next N ticks and only start accumulating `spent` once tick (1+N) has been observed. `ticks` still means ticks measured after that point. The JSONL row gains `"skip": N` and `tick_from` must be the tick at which measurement actually started. Abort reasons and `max_prompts` semantics unchanged.

2. `--burst K` (default 0): during the measured phase, once per measured tick, fire K prompts concurrently (threads over the `run` callable; each `run(index)` must still get a unique index so prompts stay unique), then do one settle sleep and one meter read, then fall back to single prompts until the next tick. Only burst when doing so cannot overshoot: if K is larger than the expected remaining prompts in the current tick, shrink it (compute expected prompts per tick from `--expect-tokens-per-pct` if given, else skip the burst for that tick). Add `--expect-tokens-per-pct` (float, optional). Readings must record `"burst": K_used` on the reading that followed a burst, and `spent` must sum every prompt in the burst. Tokens per 1% is still total spend over measured ticks divided by ticks advanced. If a burst does overshoot past the intended final tick, the run is still valid (the extra percent is simply included in the division) but must set `"overshoot": true` on the row.

3. `--settle SECONDS` (default 60, the current value) exposed on the CLI and stored in the row as `"settle_s"`.

Document the three flags in the module docstring and in bin/probe.sh comments (do not change the flags bin/probe.sh actually passes; Jonathan decides the production flags after a live comparison). Add tests: skip discards the right spans; burst fires K concurrent runs with unique indexes and sums their spend; burst shrinks or is skipped near a boundary; overshoot flagged; settle stored. Keep existing tests untouched unless a signature change forces a minimal edit.

When done: run the full unit test suite (it is small and fast), commit in normal prose with no AI attribution, `git push -u origin probe-modes`, and write a short report to out/probe-modes-report.md in the worktree (what changed, test count, exact command lines Jonathan should use for (a) a 2.5% run: skip 1, ticks 1, and (b) the same with a burst). Print the report as your final answer.
